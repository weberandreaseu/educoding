<% flash.each do |type, message| %>
  <div class="alert <%= bootstrap_flash_message(type) %> fade in">
    <div class="container">
      <button class="close" data-dismiss="alert">×</button>
      <%= message %>
    </div>
  </div>
<% end %>