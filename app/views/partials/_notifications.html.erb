<% flash.each do |type, message| %>
  <div class="alert alert-<%= type.to_s %> fade in">
    <div class="container">
      <button class="close" data-dismiss="alert">×</button>
      <%= message %>
    </div>
  </div>
<% end %>